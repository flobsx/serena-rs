[package]
name = "serena-editor"
version = "0.1.0"
edition = "2024"
description = "Serena.rs — Symbolic code editing (insert, replace, delete symbols)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
regex = "1"
serena-util = { path = "../serena-util" }
