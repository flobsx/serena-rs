//! File tools — read, create, list, search, replace files.

pub fn register() {
    // TODO: register file tools (read_file, create_text_file, list_dir,
    //       find_file, replace_content, replace_in_files, delete_lines,
    //       insert_at_line)
}
