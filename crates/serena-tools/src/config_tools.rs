//! Config/System tools — project activation, config queries, dashboard.

pub fn register() {
    // TODO: register config tools (activate_project, remove_project,
    //       get_current_config, restart_language_server, open_dashboard)
}
