[package]
name = "serena-util"
version = "0.1.0"
edition = "2024"
description = "Serena.rs — Shared utilities (text, path, fs, token counting)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiktoken-rs = "0.6"
regex = "1"
tracing = "0.1"
