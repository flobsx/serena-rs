[package]
name = "serena-config"
version = "0.1.0"
edition = "2024"
description = "Serena.rs — YAML/JSON config loading, validation, project config"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
schemars = "0.8"
tracing = "0.1"
serena-util = { path = "../serena-util" }
